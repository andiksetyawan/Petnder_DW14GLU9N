import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#f50057',
    },
    secondary: {
      main: '#f50057',
    },
    // error: {
    //   main: red.A400,
    // },
    // background: {
    //   default: '#fff',
    // },
    type:'light'
  },
});

export default theme;